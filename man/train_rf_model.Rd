% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_rf_model.R
\name{train_rf_model}
\alias{train_rf_model}
\title{Entraîner un modèle Random Forest}
\usage{
train_rf_model(
  data,
  target = "shannon",
  predictors = NULL,
  ntree = 500,
  test_split = 0.3,
  seed = 42
)
}
\arguments{
\item{data}{Data.frame contenant les variables explicatives et la variable cible.}

\item{target}{Nom de la colonne cible (indice de biodiversité). Défaut: "shannon".}

\item{predictors}{Vecteur de noms de colonnes explicatives.
Si NULL, utilise toutes les colonnes numériques sauf \code{target}.}

\item{ntree}{Nombre d'arbres. Défaut: 500.}

\item{test_split}{Proportion des données pour la validation (0-1). Défaut: 0.3.}

\item{seed}{Graine aléatoire. Défaut: 42.}
}
\value{
Une liste contenant : model (randomForest),
importance (data.frame), train_data et test_data.
}
\description{
Modélise la relation entre les pratiques agricoles / métriques paysagères
et la biodiversité via un algorithme Random Forest.
}
\examples{
# Données simulées
set.seed(1)
df <- data.frame(
  shannon       = runif(50, 0, 2),
  fragmentation = runif(50),
  prop_naturel  = runif(50),
  prop_agri     = runif(50)
)
model_result <- train_rf_model(df, target = "shannon")
print(model_result$importance)
}
