% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster_landscapes.R
\name{cluster_landscapes}
\alias{cluster_landscapes}
\title{Classifier les paysages agricoles par clustering}
\usage{
cluster_landscapes(
  landscape_metrics,
  n_clusters = 3,
  variables = c("fragmentation", "shannon_paysage", "prop_naturel"),
  seed = 42
)
}
\arguments{
\item{landscape_metrics}{Data.frame de métriques paysagères
(issu de \code{calculate_landscape_metrics}).}

\item{n_clusters}{Nombre de clusters souhaités. Défaut: 3.}

\item{variables}{Variables à utiliser pour le clustering.
Défaut: c("fragmentation", "shannon_paysage", "prop_naturel").}

\item{seed}{Graine aléatoire pour la reproductibilité. Défaut: 42.}
}
\value{
Le data.frame d'entrée enrichi d'une colonne \code{cluster}
(entier de 1 à n_clusters) et d'une colonne \code{cluster_label}.
}
\description{
Applique un algorithme K-means pour regrouper les zones/parcelles en
types de paysages homogènes selon leurs métriques paysagères.
}
\examples{
\dontrun{
lc <- import_landcover()
metrics <- calculate_landscape_metrics(lc)
# Dupliquer pour avoir assez de lignes pour le clustering
metrics_multi <- metrics[rep(1, 10), ]
metrics_multi$fragmentation <- runif(10)
result <- cluster_landscapes(metrics_multi, n_clusters = 2)
print(result[, c("zone", "cluster", "cluster_label")])
}
}
